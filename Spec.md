# Spec.md — 座席表作成アプリ 要件定義

## 1. 概要
会場にテーブルを配置し、各テーブルに椅子を配置し、各椅子にメンバー（参加者）を割り当てることで座席表を作成できる Web アプリケーションを提供する。

## 2. 用語定義
| 用語 | 説明 |
|---|---|
| 会場 (Venue) | 座席表全体を表す1つの作業エリア。本アプリでは単一の会場のみを扱う。 |
| テーブル (Table) | 会場内に配置するオブジェクト。0〜6個配置できる。 |
| 椅子 (Seat) | テーブルに配置するオブジェクト。1テーブルあたり0〜8個配置できる。 |
| メンバー (Member) | 椅子に割り当てる参加者情報。名前とX(旧Twitter)のIDを持つ。 |

## 3. 機能要件

### 3.1 会場
- 会場は1つのみとし、画面上に矩形のエリアとして表示する。
- 会場全体をリセットする機能（すべてのテーブル・椅子・メンバー情報を削除）を提供する。

### 3.2 テーブル
- 会場には0〜6個のテーブルを配置できる。
- テーブル数が6個のとき、それ以上のテーブル追加はできない（追加ボタンを無効化する）。
- テーブルには名前を設定できる（自由記述、未入力時は「テーブルN」のようなデフォルト名を付与）。
- テーブルは会場内で自由な位置にドラッグ＆ドロップで移動できる。
- テーブルは削除できる。削除すると、そのテーブルに紐づく椅子・メンバー情報も削除される。
- テーブル作成時に初期の椅子数（0〜8）を指定できる。

### 3.3 椅子
- 各テーブルには0〜8個の椅子を配置できる。
- 椅子はテーブルの周囲に均等な間隔で自動配置（円形レイアウト）される。
- テーブルごとに椅子を1個ずつ追加・削除できる（0〜8の範囲でボタンを活性/非活性制御する）。
- 椅子を削除する場合、その椅子に割り当てられていたメンバー情報も削除される。

### 3.4 メンバー
- 椅子をクリックすることで、その椅子にメンバーを登録できる。
- メンバー情報は以下を持つ。
  - 名前（必須）
  - X の ID（任意、`@` の有無は問わない）
- 既にメンバーが割り当てられている椅子は、名前が椅子上に表示される。
- メンバー情報は編集・削除（割り当て解除）できる。
- 名前が未入力の場合は保存できない（バリデーションエラーを表示する）。

### 3.5 データ永続化
- 作成した座席表（テーブル・椅子・メンバー情報・テーブル位置）はブラウザの `localStorage` に保存し、ページを再読み込みしても復元されること。

## 4. 非機能要件
- 追加のビルド処理を必要とせず、`index.html` をブラウザで開くだけで動作すること（サーバ不要）。
- 外部フレームワーク・ライブラリに依存しない（Vanilla HTML/CSS/JavaScript）。
- PC のブラウザ（最新の Chrome / Firefox / Safari / Edge）で動作すること。
- 画面幅の狭い端末（スマートフォン等）でも、閲覧・最低限の操作ができるようレイアウトが崩れないこと。
- 誤操作防止のため、上限・下限に達した操作（テーブル7個目の追加、椅子9個目の追加など）はUI上抑止する。

## 5. 対象外（スコープ外）
- 複数会場の管理・切り替え
- ユーザー認証・複数ユーザーでの共同編集
- サーバサイドでのデータ保存・共有機能
- テーブル形状（円形以外）の選択
- 印刷用レイアウトの最適化
